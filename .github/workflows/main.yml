# name of workflow/pipeline
name: github action demo pipeline

# trigger event
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: print message
        run: echo "welcome to github actions"
